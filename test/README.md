# TronBox Tests

This directory is reserved for TronBox JavaScript tests.

The previous Foundry Solidity tests were removed when the repo switched to TronBox as the TRON deployment toolchain.

Use `tronbox test --network development` for local TRE-based tests, or `tronbox test --network <network>` when targeting a configured TRON network.